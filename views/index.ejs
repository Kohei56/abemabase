<% extend('layout') %>
<div class="col-xs-6 col-lg-6 sampleEp-box float-left">
  <span class="title-box">こんなエピソードもあります。（ランダムで<%= meta.length %>件表示）</span>
  <% for(let i = 0; i < meta.length; i++) { -%>
        <div class="sampleEp"><a href=/abemabase/detail/<%= meta[i]._id %>><%= meta[i].epTitle %></a></div>
  <% } -%>
  </ul>
</div>
